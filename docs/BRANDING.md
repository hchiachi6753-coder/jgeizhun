# 換品牌教學

只需修改一個檔案，即可將網站換成你的品牌。

---

## 設定檔位置

```
src/config/site.ts
```

---

## 可修改項目

### 1. 基本資訊

```typescript
export const siteConfig = {
  name: "好運大師",           // ← 改成你的品牌名
  tagline: "古籍智慧 × AI 命理",   // ← 改成你的標語
  description: "...",         // ← 網站描述（SEO 用）
  keywords: "...",            // ← 搜尋關鍵字
```

### 2. 首頁文案

```typescript
  hero: {
    subtitle: "✦ 八字命理 · 紫微斗數 ✦",  // ← 副標題
    slogan: "融合千年古籍智慧...",         // ← 宣傳語
  },
```

### 3. 特色區塊

```typescript
  features: {
    title: "千年智慧 · 現代詮釋",    // ← 特色區標題
    subtitle: "結合《窮通寶鑑》...",  // ← 特色區副標
  },
```

### 4. 古籍引言

```typescript
  quote: {
    text: "氣若定，命自轉；心若亂，運難通。",  // ← 引言文字
    source: "神感派命理心法",                   // ← 出處
  },
```

### 5. Footer

```typescript
  footer: {
    text: "好運大師 · 融合古籍智慧的 AI 命理平台",
    credit: "Made with ✨ by JJ & J1 🦞",  // ← 改成你的名字
  },
```

### 6. 配色

```typescript
  colors: {
    primary: "#7c3aed",      // ← 主色（紫色）
    secondary: "#ffd700",     // ← 輔色（金色）
  },
```

---

## 更換 Logo

1. 準備你的 Logo 圖片：
   - `icon-192.png`（192x192 像素）
   - `icon-512.png`（512x512 像素）

2. 放到 `public/icons/` 資料夾

3. 修改 `public/manifest.json` 中的 `name` 和 `short_name`

---

## 修改完成後

1. 本地測試：
```bash
npm run dev
```

2. 確認無誤後推送：
```bash
git add .
git commit -m "更換品牌"
git push
```

3. Vercel 會自動重新部署

---

## 範例：改成「命理小站」

```typescript
export const siteConfig = {
  name: "命理小站",
  tagline: "你的專屬命理顧問",
  // ...
  footer: {
    text: "命理小站 · 科技算命新體驗",
    credit: "Made with ❤️ by 小明",
  },
};
```

---

*文件版本：v1.0 | 最後更新：2026-02-21*
